import type { Metadata } from 'next'; import Client from './Client'; export const metadata: Metadata = { title: 'subscription-audit | Setwise Digital', description: 'Free interactive tech tool for adults 45+. Plain-English, no jargon. Setwise Digital.', alternates: { canonical: 'https://setwisedigital.com/tools/subscription-audit' } }; export default function Page() { return <Client/>; }
